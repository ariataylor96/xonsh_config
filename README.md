Should work on a fresh `xonsh` installation with no modifications or package installation required.

Just run `install.sh` the first time you clone the repository, and `git pull` to get all updates from there.
